name: Publish to VS Marketplace

on:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Detect version bump
        id: version
        run: |
          BEFORE="${{ github.event.before }}"
          if [ "$BEFORE" = "0000000000000000000000000000000000000000" ]; then
            echo "changed=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          OLD=$(git show "$BEFORE:package.json" | jq -r .version)
          NEW=$(jq -r .version package.json)
          if [ "$OLD" = "$NEW" ]; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
            echo "Version unchanged ($NEW); skipping publish."
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
            echo "Version $OLD -> $NEW; will publish."
          fi

      - name: Install dependencies
        if: steps.version.outputs.changed == 'true'
        run: npm ci

      - name: Verify VSCE_PAT secret
        if: steps.version.outputs.changed == 'true'
        env:
          VSCE_PAT: ${{ secrets.VSCE_PAT }}
        run: |
          if [ -z "$VSCE_PAT" ]; then
            echo "::error::VSCE_PAT is missing. Add it under Settings → Secrets and variables → Actions (repository secret, exact name VSCE_PAT)."
            exit 1
          fi

      - name: Publish extension
        if: steps.version.outputs.changed == 'true'
        run: npx @vscode/vsce publish -p "${{ secrets.VSCE_PAT }}"
